/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Standalone output for production: smaller deploy, run with node server.js
  output: 'standalone',
};

export default nextConfig;
